.button
	sprite($zoom)
	sprite_extras()

.teste
	background-image url($base_img + '/sprite.png')
	@extend .sumir
	@extend .block
	width 50px
	height 50px
	transition 0.2s ease-out
	&:hover
		opacity 0.4

